### Prefix and Suffix

- when you are using jsp and thymleaf and all those
- When you have are sending 
like result.jsp


if in future if you decide to move from jsp to another 
it would be hassel to change everywhere


so use abstraction for it

Here we are using Preffix and Suffix

Prefix: location ( like search in this folder )
suffix: extention ( what extenstion)


And you can find all this is in 
Sring Common application properties 
or AI


### Model And View

Spring has somehting known as View Resolver

either it takes returned file name in String or View object


So when using ModelView object

return modelview object


To add Key Value Pairs

mv.addObject("result", result);


### Model Attribute


So when I am sending multiple request parametrs
the code becoem bloated so 
Instead of asceepting request parameter one by one


To achive that use 
@ModelAttribute

@ModelAttribute("ModelClassVariable should be accessed via jsp") ModelClassType ModelClassVariable

providing that it has getter and setter for all the fields


and no need of storing in Model Object

just return the jsp page name 

and in jsp 
use the same ${ModelClassVariable}


and you can skip and no need of diffrent naming 
dont use @ModelAttribute in parameter 


And adding new data inside the method
apart from The model class if you want to add new data in jsp

use method level ModelAttribute

@ModelAttribute("varname in jsp used" )
function(){

}