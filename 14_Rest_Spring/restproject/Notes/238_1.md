### @RestController

by default if we use @COntroller
the method it thinks returns the view file 
to mention expicitly 
we use @RequestBody

If every method is rest use
@RestController instead of @Controller


### Cross Origin Policy
To st cross origin policy
And use @CrossOrigin(origins = "")