### What is REST

Representational State Transfer (REST)

Entities Current Info at the given point (STATE)
aksing this current state to client
So transfering it in proper manner

Representing the state and then transfer
Representational State Transfer (REST)


### REST is an Architecture
- we use it in stateless
- meaing every request is stateless dont rember who you are
( login, is it you, why u are asking, and other stuff)


1. Can be represntion in 2 ways
    - JSON (new way)
    - XML (old way)


REST uses HTTP
HTTP has methods

2. main 4
    - GET
    - POST
    - PUT
    - DELETE