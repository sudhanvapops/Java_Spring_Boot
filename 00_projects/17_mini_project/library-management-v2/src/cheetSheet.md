### Methods I commnly used

findById(id).orElseThrow(...)
findAll(Pageable)
save(entity)
delete(entity)

ResponseEntity.ok(...)
ResponseEntity.status(...)

@Valid
@NotNull
@NotBlank

@Transactional

stream().map(...).toList()



Lazy vs Eger Loading

