Design method signatures only

### BorrowerDAO

👉 What do you need?
Not everything — only what service requires.

Think:
    How will you find borrower?

based on card number
findBorrower(cardNumber)

but also if we need to find one particular one
findBorrowerByName() // returns list and we can choose one by further instructions

### BookDAO

Think:
    How will you identify a book?

Actually identifying a book in two ways 
one the perosn borrowing asks by its name
second when he gets it we put isbn in our LMS to find that particular book for futher operaations


so:
findBookByISBN() // returns one book
findBookByName() // returns list of books



### BorrowRecordDAO

Think:
    Do you need find methods? or just save?

We need both 

findBorroweRecord(cardNumber,isbn) // gives a particular book record
findBorrowRecords(cardNumber) // gives a set of all book records

saving
saveBorrowRecord(borrowRecord) // saves a particular brrowed bookrecord
# DAO should not know cardNumber


### Fix Each DAO
void save(BorrowRecord record);

List<BorrowRecord> findByBorrower(Borrower borrower);
List<BorrowRecord> findActiveByBook(Book book); // optional


Avoid returning null