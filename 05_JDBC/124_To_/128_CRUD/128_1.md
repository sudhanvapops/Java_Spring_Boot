### Create 

using  pstmt.executeUpdate()
to insert

1. Build query
String query2 = "INSERT INTO student  VALUES (?, ?, ?)";

2. PrepareStatemt
PreparedStatement pstmt = conn.prepareStatement(query2);

3. set ? using setMethods

4. execute 


you create a Statement / PreparedStatement object for each SQL query execution.

Connection  →  Statement / PreparedStatement  →  Execute Query

Execute many times using SAME object
You don’t need a new object if query structure is same.

