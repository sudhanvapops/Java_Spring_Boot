### Execute

select * from student where id = 1
select NAME from student where id = 1


ResultSet → pointer moving row by row
true  → row exists
false → finished

rs.next() put the pointer to next row

To print entire table:
You need to get everyting with its datatype

int id = rs.getInt("id");
String name = rs.getString("name");
int age = rs.getInt("age");

No other option

pointer will be before the first row
so rs.next() to set pointer to next

### Print dynamically (VERY POWERFUL)

- Column index starts from 1
while (rs.next()) {
    for (int i = 1; i <= cols; i++) {
        System.out.print(rs.getObject(i) + " ");
    }
    System.out.println();
}

### Use Prepared Statement 

Statement = SQL Injection risk ❌
PreparedStatement = Industry standard ✅