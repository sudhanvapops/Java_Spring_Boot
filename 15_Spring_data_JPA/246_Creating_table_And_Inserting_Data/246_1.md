### Using JPA

- Make a repo layer 
- make it an interface
- extends JpaRepository<>

- <ModelName,PrimaryKey dataType>


@Repositriy 


@Entity 
is a JPA annotation that tells Spring Data JPA:
"This Java class represents a table in the database."


@Id
To make a filed ID

so sinc We are using JPA

all the same anotations of Hibernate casue it is also extends JPA

