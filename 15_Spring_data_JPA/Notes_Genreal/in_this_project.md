### IN this Project

- we are going to use spring data jpa on top of hibernate

- postgres sql

- and not using service layer for simplcity

- set up database properties in applicatio.properties

@Entity is a JPA annotation that tells Spring Data JPA:

"This Java class represents a table in the database."



### 

- spring.jpa.hibernate.ddl-auto=create

create - make table every time
update make table if not other wise use it 

- spring.jpa.show-sql=true


