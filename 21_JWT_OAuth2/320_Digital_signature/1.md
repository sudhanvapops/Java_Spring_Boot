### Securing Data

A
A: private 
B: public key

B
B: private 
A: public key

A -> enc msg(B public key)
B -> dec msg(B private key)

problem
haker can send another msg 
using B public key

### Signing

A
A: private 
B: public key

B
B: private 
A: public key

A -> enc msg(A private key)
B -> dec msg(A public key)

if any one tries another packet
they have to sign it using private key
thats not posibble

<!-- ! Decription of data can be done -->
msg content can be seen 
only signing done


proof of A sent is beacuse 
A is encription doen only using A private key

Any one can read data


### So so doing double encrytion combining both


A
A: private 
B: public key

B
B: private 
A: public key

now flow

A -> enc msg(B public key)
same packet -> enc msg(A private key)

proof that a has sent it
B -> dec msg(A pulic key) // verify signature
same packet -> dec msg(B private key) // verify data

C -> if catches paket in middle
C -> dec using A public key
c -> same packet enc with B public key
only way to decrypt it is using B's Private key

