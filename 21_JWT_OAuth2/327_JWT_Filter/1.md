### JWT Filter


- this step is for verifying signature of a jwt
- after login 

- here by default uses usernamepasswordAuthenticationFIlter
- now we are adding it with our  jwt filter before it
- for how to extratc the data and verify


### Request Filter

HTTP Request
      │
      ▼
Security Filter Chain
      │
      ├── CorsFilter
      ├── JwtAuthenticationFilter   <-- Your custom filter
      ├── UsernamePasswordAuthenticationFilter
      ├── AuthorizationFilter
      └── ...
      │
      ▼
Controller

.addFilterBefore(myfilter,before which filetr) in securityConfig file