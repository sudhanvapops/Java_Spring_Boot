### Cheet Sheet


### For Adding


preparedStatment not exactly but sql query String i ps syntax


update(preparedStatment,values)

### For Query

A RowMapper in Spring JDBC is an object that converts one database row into one Java object.

Database Row
     ↓
RowMapper
     ↓
Student Object


we use query(preparedStatment,RowMapper)