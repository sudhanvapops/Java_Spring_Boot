### JDBC Template

JdbcTemplate is a Spring class that makes working with JDBC much easier.
It removes most of the repetitive JDBC code.


You must manually handle:
    connections
    statements
    result sets
    exceptions
    closing resources

This is called boilerplate code.


JdbcTemplate simplifies this


We have two methods

execute and update for CUD
and execute query for R