Add @Id for making it promary key

and after that run it wont save cause

dbms priciple ACID
every time you save data its a transaction

- saving means transaction
- after saving commit the transaction

Transaction tr = session.beginTransaction()
tr.commit()

<!-- Code -->
Transaction tr = s.beginTransaction();
session.persist(s1);
tr.commit();


Main
 ↓
HibernateUtil loads
 ↓
SessionFactory created
 ↓
Session opened
 ↓
Transaction started
 ↓
Entity persisted
 ↓
Commit