Configuration cfg = new Configuration(); // from hiberanate

SessionFactory sf = cfg.builSessionFactory(); gives factory obj
- us eonly once its like connection



<!-- Usiing above craete multiple session -->
use Session s = sf.openSession();
for any new operation 
use this


<!-- ! Save -->
s.persist(s1)