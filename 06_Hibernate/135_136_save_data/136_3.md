Auto manage table
if no create
if yes us eit
<property name="hibernate.hbm2ddl.auto">update</property>

- in production update makes sense

meaning if schema changes the table is updated
but old column will be there
so in devlopment its not best choice