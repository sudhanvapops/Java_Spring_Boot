### We can configure in two ways

XML and annotations

### XML

- generate it via ai

- put XML in resources
- default name:
    hibernate.cfg.xml

### Steps

hibernate.cfg.xml
        ↓
Configuration
        ↓
SessionFactory
        ↓
Session
        ↓
Transaction
        ↓
Database


### Annotaed class

cfg.addAnnotatedClass(com.orm.Student.class);

in StudentClass
add 
@Entity from jakarta -> JPA (jakarat persistance API)
JPA 
think of it as a rules that every orm should follow

@Enitity menas now ibernate automatically manages it
