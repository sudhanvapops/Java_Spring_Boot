In JDBC We dont get Cahce 
In Hibernate we get Cahce

its an advantage 

Two diffrent Level of cache
L1 and L2

and when you call .find()
and the object is in same session
it wont execute find() query to DB
instead it prints from object itslef

and L1 cache is used when it is called in same session


Hibernate: select l1_0.Programmer_pid,l1_1.lid,l1_1.brand,l1_1.model,l1_1.ram from Programmer_Laptop l1_0 join Laptop l1_1 on l1_1.lid=l1_0.laptops_lid where l1_0.Programmer_pid=?

The above query is called only when it is done in diffrent session


### Next

when you have intenstion where you want multiple seesion should share same data
then These library can be checked out
caffiene or ehCache to use L2 cache


By default hibernate uses Lazy fetch
when you have collections entity
it uses Lazy Fetch by default 
means When fethed it wont fire to get Laptop it only does for Programmers

it also fetches laptop when you print the programmer
why it does beacuse what if you have too many data and its of no use


### Eger

when you want eger fetch
go to OneToMany(fetch = FetchType.Eger)