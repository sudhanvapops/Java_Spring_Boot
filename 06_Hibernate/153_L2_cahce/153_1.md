### L2 Cahce

using EH cache

If same query fired multiple times it uses L1 cahce to Get the data
If you open a new session and run the same query it will fire another query 
If we implement L2 cahce then the s2 have got the data from L1 cache


use jcahce 
and EHcache

have to do some chancges in hibrnate config fie chatgpt it 

Step 1 — Add dependency (Maven)
Step 2 — Enable cache in configuration
Step 3 — Enable caching for entity

Application
     ↓
Session (First Level Cache)
     ↓
Second Level Cache
     ↓
Database