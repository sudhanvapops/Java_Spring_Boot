### Get and load
### lazy fetching and eger fetching

load is depricated 

This uses lazy loading.
and it wont fire a query until it uses
Laptop l = session.getReference(Laptop.class,2);

At this moment:

❌ No SQL query executed
✔ Only a proxy object created


### Compare with get()
Laptop l = session.get(Laptop.class, 102);

This is eager loading
Hibernate immediately runs SQL
The object is fully loaded

